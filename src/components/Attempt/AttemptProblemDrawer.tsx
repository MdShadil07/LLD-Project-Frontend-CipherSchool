import { BookOpen, Check, X } from 'lucide-react';

const requirements = ['Multiple floors and parking spots', 'Different vehicle types and suitable spots', 'Ticket generation on entry', 'Payment at automated exit or attendant', 'Capacity limits and full-lot messaging'];

export default function AttemptProblemDrawer({ open, onClose }: { open: boolean; onClose: () => void }) {
  if (!open) return null;
  return <><button type="button" aria-label="Close problem details" onClick={onClose} className="fixed inset-0 z-[100] cursor-default bg-slate-950/25 backdrop-blur-sm" /><aside className="fixed inset-y-0 right-0 z-[101] flex w-full max-w-xl flex-col border-l border-slate-200 bg-white shadow-2xl"><div className="flex items-center justify-between border-b border-slate-100 px-6 py-5"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-indigo-100 text-indigo-600"><BookOpen className="h-4 w-4" /></span><div><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Problem brief</p><h2 className="text-lg font-bold text-slate-900">Design a Parking Lot</h2></div></div><button type="button" onClick={onClose} aria-label="Close" className="rounded-full p-2 text-slate-400 hover:bg-slate-100 hover:text-slate-700"><X className="h-5 w-5" /></button></div><div className="flex-1 overflow-y-auto p-6"><p className="text-sm leading-relaxed text-slate-600">Design a parking lot that handles different vehicle types, multiple floors, ticketing, payment, and efficient spot allocation.</p><h3 className="mt-8 text-xs font-bold uppercase tracking-wider text-slate-400">Required behavior</h3><ul className="mt-3 space-y-3">{requirements.map((requirement) => <li key={requirement} className="flex gap-2 text-sm text-slate-600"><Check className="mt-0.5 h-4 w-4 shrink-0 text-emerald-500" />{requirement}</li>)}</ul><div className="mt-8 rounded-xl border border-indigo-100 bg-indigo-50 p-4"><p className="text-xs font-bold text-indigo-800">Evaluation focus</p><p className="mt-1 text-sm leading-relaxed text-indigo-700">Explain responsibility assignment, coupling, extensibility, core behavior, and edge-case handling.</p></div></div><div className="border-t border-slate-100 p-6"><button type="button" onClick={onClose} className="w-full rounded-xl bg-slate-900 py-3 text-sm font-semibold text-white hover:bg-slate-800">Back to design</button></div></aside></>;
}
