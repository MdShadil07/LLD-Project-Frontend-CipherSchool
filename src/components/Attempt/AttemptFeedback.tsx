import { AlertTriangle, CheckCircle2, RotateCcw } from 'lucide-react';
import type { AttemptRecord } from './attempt.types';

export default function AttemptFeedback({ attempt, onRetry }: { attempt: AttemptRecord; onRetry: () => void }) {
  const results = attempt.results ?? [];
  return <section className="space-y-5"><div className="flex flex-col justify-between gap-4 rounded-2xl border border-emerald-100 bg-emerald-50 p-5 sm:flex-row sm:items-center"><div><div className="flex items-center gap-2 text-emerald-700"><CheckCircle2 className="h-5 w-5" /><span className="text-sm font-bold">Evaluation completed</span></div><p className="mt-2 text-sm text-emerald-800">Your submission was evaluated against the Parking Lot rubric. Review the evidence before your next attempt.</p></div><button type="button" onClick={onRetry} className="inline-flex items-center justify-center gap-2 rounded-lg bg-white px-4 py-2.5 text-sm font-semibold text-emerald-700 shadow-sm hover:bg-emerald-100"><RotateCcw className="h-4 w-4" />Retry attempt</button></div><div className="grid gap-4 sm:grid-cols-2">{results.map((result) => <article key={result.criterion} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><h3 className="font-bold text-slate-800">{result.criterion}</h3><span className="rounded-full bg-indigo-50 px-2.5 py-1 text-xs font-bold text-indigo-700">{result.score}/10</span></div><p className="mt-4 text-xs font-bold uppercase tracking-wider text-slate-400">Evidence</p><p className="mt-1 text-sm leading-relaxed text-slate-600">{result.evidence}</p><div className="mt-4 flex gap-2 rounded-lg bg-amber-50 p-3 text-xs leading-relaxed text-amber-800"><AlertTriangle className="mt-0.5 h-4 w-4 shrink-0" />{result.concern}</div><p className="mt-4 text-sm font-medium leading-relaxed text-indigo-700">Next step: {result.suggestion}</p></article>)}</div></section>;
}
